function sum(a, b) {
    return a+b;
}

// console.log(sum(sum(3, 4), 3));

function isAdult(age) {
    if(age >= 18) {
        return "adult";
    } else {
        return "not adult";
    }
    console.log("bye bye");
}